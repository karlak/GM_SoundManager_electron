{
  "name": "gm_soundmanager",
  "version": "0.1.0",
  "description": "GM SoundManager",
  "main": "main.js",
  "scripts": {
    "start": "cd gm_music_lib && node-gyp rebuild && electron-rebuild && cd .. && electron .",
    "test": "cd gm_music_lib && node-gyp rebuild"
  },
  "license": "MIT",
  "dependencies": {
    "bindings": "^1.2.1",
    "nan": "^2.6.2"
  },
  "gypfile": true,
  "devDependencies": {
    "electron": "^1.6.11",
    "electron-rebuild": "^1.6.0"
  }
}
